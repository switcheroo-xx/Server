Server
======
Web server for communicating with tellstick

Before running first time
-------------------------
Run "sh configure_https.sh" and input data to setup keys and certificates for HTTPS.

Run "sh configure_password.sh" to set the password required from clients. This password will be salted and hashed iteratively before storing.

To run
------
Run "sh run.sh <PORT_NBR>" to start the server on port PORT_NBR

Requirements:
Requires node.js

